using System;

namespace hw3_2
{
    class Program
    {
        static void Main(string[] args)
        {
            int i = 0;
            for (i = 1; i < 11;i ++)
            {

                Random rnd = new Random();
                int value = rnd.Next();
                Console.WriteLine(value);

            }

            
        }
    }
}
namespace hw3._3
{
    class Program
    {
        static void Main(string[] args)
        {
            int i = 0;
            for (i = 1; i < 11; i++)
            {

                Random rnd = new Random();
                /// int value = rnd.Next(0, 10);
                // int value = rnd.Next(20, 50);
                int value = rnd.Next(-10, 10);
                Console.WriteLine(value);
            }
        }
    }
}

namespace Hw3._6
{
    class Program
    {

        static int GetRandom()

        {
            Random n = new Random();

            int number = n.Next(3, 15);
            return number;

        }
        static void Main(string[] args)
        {
            int n = GetRandom();


            for (int i = 1; i < n; i++)
            {

                Random rnd = new Random();

                int value = rnd.Next(-10, 35);
                Console.WriteLine(value);
            }
        }
    }
}

